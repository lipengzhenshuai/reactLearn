export enum WordType {
  UPDOWN = 0,
  LEFTRIGHT,
  FOURLINE,
  SQUARE,
  COMBINE,
}

export enum PinYinType {
  FORM = 0,
  FIRSTUP,
  ALLUP,
}

export enum PinYinFont {
  PYFont=1,
  PYColor,
  PYSize,
}

export enum WordFont {
  WFont=4,
  WColor,
  WSize,
}
